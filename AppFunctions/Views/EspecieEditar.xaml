<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppFunctions.Views.EspecieEditar"
             Title="{Binding espNome}">
    
    <VerticalStackLayout
            Padding="20,0"
            Spacing="15">

        <Label 
            x:Name="lblCodigo" 
            Text="Código:"/>
        
        <Entry 
            x:Name="etrCodigo" 
            IsEnabled="False" 
            Text="{Binding espId}"/>

        <Label 
            x:Name="lblNome" 
            Text="Nome:"/>
        
        <Entry 
            x:Name="etrNome" 
            Text="{Binding espNome}"/>

        <Button 
            x:Name="btnAlterar" 
            Text="Alterar" 
            Clicked="btnAlterar_Clicked"/>
        
    </VerticalStackLayout>
    
</ContentPage>